<h2><%= @patient.name %></h2>
<p>
  Age: <%= @patient.age %>
</p>
Comments:
  <% @patient.comments.each do |comment| %>
    <%= link_to comment.user.username, user_path(comment.user) %> said
    <%= comment.content %>
  <% end %>
